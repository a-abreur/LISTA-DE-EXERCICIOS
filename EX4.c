#include <stdio.h>

int main()
 {

    int numero;
    int conta = 0;
    int adicao;
    int subtracao;
    int mult;
    float div;

    printf("Digite um numero para o calculo: ");
    scanf("%d", &numero);

    printf("\nTabuada do + e do - para o numero %d", numero);

    conta = conta + 1;
    adicao = numero + conta;
    subtracao = numero - conta;
    printf("\n%d + %d = %d        %d - %d = %d", numero, conta, adicao,numero,conta,abs(subtracao));

    conta = conta + 1;
    adicao = numero + conta;
    subtracao = numero - conta;
    printf("\n%d + %d = %d        %d - %d = %d", numero, conta, adicao,numero,conta,abs(subtracao));

    conta = conta + 1;
    adicao = numero + conta;
    subtracao = numero - conta;
    printf("\n%d + %d = %d        %d - %d = %d", numero, conta, adicao,numero,conta,abs(subtracao));

    conta = conta + 1;
    adicao = numero + conta;
    subtracao = numero - conta;
    printf("\n%d + %d = %d        %d - %d = %d", numero, conta, adicao,numero,conta,abs(subtracao));

    conta = conta + 1;
    adicao = numero + conta;
    subtracao = numero - conta;
    printf("\n%d + %d = %d        %d - %d = %d", numero, conta, adicao,numero,conta,abs(subtracao));

    conta = conta + 1;
    adicao = numero + conta;
    subtracao = numero - conta;
    printf("\n%d + %d = %d        %d - %d = %d", numero, conta, adicao,numero,conta,abs(subtracao));

    conta = conta + 1;
    adicao = numero + conta;
    subtracao = numero - conta;
    printf("\n%d + %d = %d        %d - %d = %d", numero, conta, adicao,numero,conta,abs(subtracao));

    conta = conta + 1;
    adicao = numero + conta;
    subtracao = numero - conta;
    printf("\n%d + %d = %d        %d - %d = %d", numero, conta, adicao,numero,conta,abs(subtracao));

    conta = conta + 1;
    adicao = numero + conta;
    subtracao = numero - conta;
    printf("\n%d + %d = %d        %d - %d = %d", numero, conta, adicao,numero,conta,abs(subtracao));

    printf("\n\nTabuada do * e do / para o numero %d", numero);
    conta = 0;
    conta = conta + 1;
    mult = numero * conta;
    div = (float)numero / (float)conta;
    printf("\n%d * %d = %d        %d / %d = %.2f", numero, conta, mult,numero,conta,div);

    conta = conta + 1;
    mult = numero * conta;
    div = (float)numero / (float)conta;
    printf("\n%d * %d = %d        %d / %d = %.2f", numero, conta, mult,numero,conta,div);

    conta = conta + 1;
    mult = numero * conta;
    div = (float)numero / (float)conta;
    printf("\n%d * %d = %d        %d / %d = %.2f", numero, conta, mult,numero,conta,div);

    conta = conta + 1;
    mult = numero * conta;
    div = (float)numero / (float)conta;
    printf("\n%d * %d = %d        %d / %d = %.2f", numero, conta, mult,numero,conta,div);

    conta = conta + 1;
    mult = numero * conta;
    div = (float)numero / (float)conta;
    printf("\n%d * %d = %d        %d / %d = %.2f", numero, conta, mult,numero,conta,div);

    conta = conta + 1;
    mult = numero * conta;
    div = (float)numero / (float)conta;
    printf("\n%d * %d = %d        %d / %d = %.2f", numero, conta, mult,numero,conta,div);

    conta = conta + 1;
    mult = numero * conta;
    div = (float)numero / (float)conta;
    printf("\n%d * %d = %d        %d / %d = %.2f", numero, conta, mult,numero,conta,div);

    conta = conta + 1;
    mult = numero * conta;
    div = (float)numero / (float)conta;
    printf("\n%d * %d = %d        %d / %d = %.2f", numero, conta, mult,numero,conta,div);

    conta = conta + 1;
    mult = numero * conta;
    div = (float)numero / (float)conta;
    printf("\n%d * %d = %d        %d / %d = %.2f", numero, conta, mult,numero,conta,div);




    return 0;
}
